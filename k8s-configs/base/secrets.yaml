# Secrets template — fill in real values before deploying
# NEVER commit real secrets to git
apiVersion: v1
kind: Secret
metadata:
  name: mindlab-secrets
type: Opaque
stringData:
  DB_USER: "mindlab"
  DB_PASSWORD: "REPLACE_ME"
  ENCRYPTION_KEY: "REPLACE_ME_32BYTE_BASE64"
  JWT_SECRET: "REPLACE_ME"
  NATS_USER: "mindlab"
  NATS_PASSWORD: "REPLACE_ME"
  REDIS_PASSWORD: "REPLACE_ME"
  STRIPE_API_KEY: "REPLACE_ME"
  OPENAI_API_KEY: "REPLACE_ME"
